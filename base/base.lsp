(defun base:info ()
  "»ù´¡¿â"
  (princ)
  )
(@:load "base/condition")
(@:load "base/format")

(setq vlap p:vlap
      stringp p:stringp
      realp p:realp
      enamep p:enamep
      variantp p:variantp
      picksetp p:picksetp
      intp p:intp
      safearrayp p:safearrayp
      ename-listp p:ename-listp
      vla-listp p:vla-listp
      string-listp p:string-listp
      dotpairp p:DotPairp
      curvep p:curvep
      )
